---
name: 恢复升级丢失的股票策略界面项目
overview: 从本地 CodeBuddy 对话历史记录中提取升级前已确认的 PySide6 股票策略系统方案（4 个 Python 文件约 1550 行代码），恢复到当前工作区，并验证可运行后继续完善。
todos:
  - id: extract-restore-code
    content: 解析对话记录 JSON，提取 4 个代码文件并写回工作区
    status: completed
  - id: verify-code-deps
    content: 用 py_compile 与 [skill:lsp-code-analysis] 验证代码语法和结构完整性
    status: completed
    dependencies:
      - extract-restore-code
  - id: check-env-interface
    content: 检查 Python/PySide6/stock_sdk 依赖与 127.0.0.1:7899 接口可用性，缺失则安装
    status: completed
    dependencies:
      - verify-code-deps
  - id: run-integration-fix
    content: 运行 main.py 联调：策略扫描、14因子评分倒序、K线图，修复运行期问题
    status: completed
    dependencies:
      - check-env-interface
  - id: write-docs
    content: 生成 README 运行说明与恢复方案摘要文档
    status: completed
    dependencies:
      - run-integration-fix
---

## 需求概述

用户升级 CodeBuddy 后，历史会话在界面中消失，担心之前与 AI 确认的"股票策略界面开发方案"丢失。经排查确认：完整对话记录（36 条 API 消息 + 74 条 UI 消息）与已生成的全部代码仍保留在本地 `tencent.planning-genie/tasks/1787329494679/` 中，可 100% 恢复。

## 核心功能

- 从本地对话记录中提取确认方案时生成的 4 个 Python 文件（strategy_data.py / factors.py / strategies.py / main.py，共约 1550 行），完整写回工作区
- 验证恢复代码可运行：语法检查、依赖（PySide6、stock_sdk）与本地数据接口（127.0.0.1:7899）可用性
- 继续完成对话中断的开发：运行 PySide6 主界面，实现股票策略选择、全市场扫描、14 因子评分倒序展示、点击查看 K 线图
- 输出项目运行说明与恢复的方案摘要

## 目标形态

按对话最终确认的方案：Python + PySide6 桌面应用（股票策略系统，对接本地 stock_sdk 数据接口，v9 九大策略 + 14 因子评分）。

## 技术栈

- Python 3 + PySide6（Qt 桌面 GUI，恢复代码中原有方案）
- stock_sdk 本地数据 SDK（数据服务默认 `127.0.0.1:7899`，行情/板块/指标/基本面，聚宽 `get_fundamentals` 语法）
- 恢复数据源：`C:/Users/wxb/AppData/Roaming/CodeBuddy CN/User/globalStorage/tencent.planning-genie/tasks/1787329494679/api_conversation_history.json`

## 实现方案

### 数据恢复（关键）

1. 用 Python 脚本解析 `api_conversation_history.json`（消息列表，每条 `content` 为 `[{"type":"text","text":"..."}]` 结构）
2. 遍历 assistant 消息，用正则匹配 `<write_to_file><path>X</path><content>...</content></write_to_file>` 标签，提取 `<path>` 与 `<content>` 原文
3. 按 UTF-8 编码将 4 个文件写回工作区 `c:/Users/wxb/CodeBuddy/20250928162354/`，保持 JSON 解析后的真实换行与缩进
4. 对比提取结果与记录中的字符数/行数（strategy_data.py 356 行、factors.py 302 行、strategies.py 413 行、main.py 480 行），确保无截断

### 分层架构（沿用已确认方案，不做改动）

```
main.py（UI 层：策略选择/扫描/表格/K线图）
  → strategies.py（策略层：9 个 v9 硬条件，get_strategies/check_strategy/risk_warnings）
  → factors.py（评分层：14 因子引擎，score_stock/FACTORS，综合分倒序）
  → strategy_data.py（数据层：MarketCache/OnlineData/load_market_rows/指标计算，对接 stock_sdk）
```

### 验证与联调

- `python -m py_compile` 校验 4 个文件语法
- 检查依赖安装状态：PySide6、pandas（如代码引用）、stock_sdk；缺失则按项目依赖补装
- 探测 `127.0.0.1:7899` 数据服务连通性，验证 stock_sdk 的 rd/行情接口可调用
- 运行 `main.py` 联调：全市场扫描（PREFIXES=`["0*","3*","6*","920*"]`）、14 因子评分、结果表格按综合分倒序、点击查看 K 线
- 仅修复运行期问题，不改变已确认的方案核心

## 性能与可靠性

- 全市场扫描约数千只股票，main.py 已用 QThread 后台扫描避免阻塞 UI，数据层带缓存（MarketCache/OnlineData），保持该设计
- 候选池初筛 limit=300 控制展示规模，避免大表渲染卡顿
- 恢复过程只读源 JSON、只写工作区新文件，不影响 CodeBuddy 自身数据目录，零风险

## 实施注意

- `api_conversation_history.json` 必须按 JSON 解析后再提取文本（content 为 JSON 转义后的真实换行），不能按原始字节正则匹配
- 代码中时间范围 `START="20260301" END="20260819"` 已过期，联调时按需更新
- 接口文档参考：`http://a.123128.xyz/docs/AI策略界面开发接口文档.md`（Python SDK 版）
- 用户原始诉求含"纯前端 HTML"，最终按对话确认的 PySide6 方案执行；如联调中用户改主意，可在本计划完成后另行规划

## Agent Extensions

### Skill

- **lsp-code-analysis**
- Purpose: 恢复 4 个代码文件后，用 LSP 语义分析验证文件结构完整性（类/函数/导入关系），辅助定位依赖缺失或跨文件引用问题
- Expected outcome: 确认 strategy_data.py / factors.py / strategies.py / main.py 的符号与导入链完整，提前发现恢复中的截断或引用断裂